<UserControl x:Class="Snake.SnakeUC"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignHeight="500" d:DesignWidth="500" Focusable="True" Loaded="Window_Loaded" MaxWidth="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" MaxHeight="{Binding ActualWidth, RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid Name="Griddy" Focusable="False"/>
    </Grid>
</UserControl>
